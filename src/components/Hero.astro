---

---

<section class="hero">
  <div class="hero-callout">
    <h2 class="callout-heading">
      Websites that <span class="attn">demand</span> attention.
    </h2>
  </div>
  <!-- <div class="backdrop">-->
    <!-- <img src="/images/Concho.png" alt="Concho River after a storm"> -->
  <!--</div> -->
  <div class="message">
    <p class="hero-message">
      Connect users to your products and services with a site that guarantees you'll stand out.
    </p>
    <a href="/#services" class="button">Let's Get Started</a>
  </div>
</section>

<style lang="scss">
  @use '../style/mixins';

  .hero {
    display: flex;
    // gap: var(--space-xs);
    // align-items: flex-end;
    padding-block: 0;
    min-width: 100%;
    color: var(--light);
    // background: var(--accent-secondary);
  }

  .hero-callout {
    flex: 1;
    z-index: 10;
    display: flex;
    justify-content: center;
    padding: var(--space-s);
    background: var(--accent-primary);
  }

  .callout-heading {
    -webkit-text-stroke: 2px var(--foreground);
    color: transparent;
    font-size: var(--font-5);
    font-weight: 300;
    font-family: var(--sans);
    line-height: 1.3;
    letter-spacing: 0.05em;
    max-width: 8ch;
    text-align: left;
    .attn {
      // color: var(--background);
      color: var(--foreground);
    }
    
    @include mixins.query(phone) {
      font-size: var(--font-4);
    }
  }

  .message {
    flex: 2;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: var(--space-s);
    padding: var(--space-l);
    // background: var(--accent-tertiary);
    background-image: url('/images/Concho.jpg');
    // background-image: linear-gradient(to left, transparent, var(--dark)), url('/images/Concho.jpg');
    background-repeat: no-repeat;
    background-size: cover;
  }

  .hero-message {
    font-size: var(--font-1);
    text-align: center;
    line-height: 1.7;
    letter-spacing: 0.075em;
    max-width: 20ch;
    color: var(--foreground);
    text-shadow: 0.125em 0.125em 0.125em hsl(var(--dark-hsl), 0.9);
  }
</style>